server:
  port: 9050

spring:
  application:
    name: student
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://io-surf:3306/Student
    username: nji
    password: 1011ADmysql!
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

eureka:
  instance:
    host: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:9030/eureka